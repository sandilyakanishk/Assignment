# Sample inventory — adjust hosts to match your environment.
# Run the playbook against this with:
#     ansible-playbook -i ansible/inventory.ini ansible/deploy.yml -e "new_tag=1.2.3"

[app_servers]
prod-app-01.example.com
prod-app-02.example.com

[app_servers:vars]
ansible_user=deploy
ansible_ssh_private_key_file=~/.ssh/deploy_key
